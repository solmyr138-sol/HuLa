# 改完需要重启的变更：必须协助重启并验证

当本次改动会影响到**正在运行的开发服务/进程**（例如 Spring Boot jar、`mvn spring-boot:run`、前端 dev server、Docker 容器等）时：

- **必须**在修改完成后，主动检查当前是否已有相关服务在运行（优先复用现有启动方式，避免重复启动）。
- 若需要重启才能生效，**必须由 Agent 协助完成重启**（按项目当前实际启动方式执行：例如 jar/脚本/docker compose 等），而不是只提示用户“去重启”。
- 重启后**必须自检**：至少验证 1 个与本次改动强相关的可观测信号（如 `actuator/health`、关键接口 curl、或前端页面关键路径不再复现）。
- 约束：仅触达本次改动所需的服务，避免无关服务的重启；不做破坏性操作（如清库/删数据）除非用户明确要求。

