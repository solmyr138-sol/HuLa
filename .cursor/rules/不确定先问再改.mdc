---
description: 业务常量、分页条数、接口参数等未经确认不得擅自修改；不确定时必须先问用户。
globs:
alwaysApply: true
---

# 不确定先问，禁止擅自改业务常量

- 修改**每页条数、默认值、金额/时间等业务常量、接口字段含义、与后端或文档相关的约定**前，若用户或文档**未明确**要求变更，必须先向用户说明**当前代码/文档中的值**，并**征得确认**后再改。禁止凭「常见做法」自行替换（例如把每页 20 改成 30）。
- 仓库根目录 **CLAUDE.md** 第 1 节已要求：*State your assumptions explicitly. If uncertain, ask.* 实施时必须遵守，不得忽略。
- 若用户已指定数值或行为，以用户为准；若用户只说「优化」而未指定常量，**保持原有常量**，除非用户当场同意新值。
